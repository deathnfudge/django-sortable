language: python
python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4
  - 3.5
install:
  - pip install -e .
  - pip install coverage
  - pip install coveralls

script: coverage run --source=django_sortable setup.py test
after_success:
  - coveralls
